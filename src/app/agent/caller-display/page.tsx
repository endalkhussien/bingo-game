'use client';

import { CallerDisplay } from '@/presentation/components/caller/caller-display';

/** Full-screen hall caller display — open in a second window or projector tab. */
export default function CallerDisplayPage() {
  return <CallerDisplay />;
}
