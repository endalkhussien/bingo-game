/**
 * Default organization recharge key — identical on every fresh install.
 * Admin can change it under Recharge Codes; agents must use the same key on their PC.
 */
export const DEFAULT_OPERATOR_ORG_KEY =
  'tebib-bingo-v1-default-operator-recharge-secret-key';
