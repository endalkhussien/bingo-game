'use client';

/** @deprecated Caller display shows called numbers — kept for older game-board imports. */
export function CalledNumbersStrip(_props: {
  called?: number[];
  lastDrawn?: number | null;
  maxBalls?: number;
  language?: string;
}) {
  return null;
}
